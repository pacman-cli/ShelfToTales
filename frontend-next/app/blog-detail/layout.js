export const metadata = {
  title: 'Article — Shelf To Tales',
  description: 'Read the full article on the Shelf To Tales blog.',
};

export default function Layout({ children }) {
  return children;
}
