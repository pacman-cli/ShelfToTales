export const metadata = {
  title: 'Reading Rooms',
  description: 'Join reading rooms to discuss books with fellow readers in real-time.',
};

export default function ReadingRoomLayout({ children }) {
  return children;
}
