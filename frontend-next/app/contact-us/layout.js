export const metadata = {
  title: 'Contact Us — Shelf To Tales',
  description: 'Reach the Shelf To Tales team — support, partnerships, and general enquiries.',
};

export default function Layout({ children }) {
  return children;
}
