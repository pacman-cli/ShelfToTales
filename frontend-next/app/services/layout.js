export const metadata = {
  title: 'Services — Shelf To Tales',
  description: 'Explore the services Shelf To Tales offers to readers, authors, and book communities.',
};

export default function Layout({ children }) {
  return children;
}
