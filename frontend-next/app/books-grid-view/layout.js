export const metadata = {
  title: 'Browse Books — Shelf To Tales',
  description: 'Explore our full catalogue of books across every genre and category.',
};

export default function Layout({ children }) {
  return children;
}
