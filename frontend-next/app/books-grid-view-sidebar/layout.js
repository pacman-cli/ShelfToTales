export const metadata = {
  title: 'Books — Filtered View — Shelf To Tales',
  description: 'Browse books by category, author, and price.',
};

export default function Layout({ children }) {
  return children;
}
