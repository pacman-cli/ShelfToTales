export const metadata = {
  title: 'Dashboard',
  description: 'Your reading dashboard — track progress, goals, and achievements.',
};

export default function DashboardLayout({ children }) {
  return children;
}
