export const metadata = {
  title: 'Pricing — Shelf To Tales',
  description: 'Subscription tiers, membership benefits, and pricing for Shelf To Tales.',
};

export default function Layout({ children }) {
  return children;
}
